<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Define the menu item -->
        <record id="menu_additional_vendors" model="ir.ui.menu">
            <field name="name">Additional Vendors</field>
            <field name="action" ref="account.res_partner_action_supplier"/>
            <field name="parent_id" ref="purchase.menu_procurement_management"/>
            <field name="sequence" eval="16"/>
        </record>

        <!-- Define the action associated with the menu item -->
        <record id="additional_vendors.action_additional_vendor" model="ir.actions.act_window">
            <field name="name">Additional Vendors</field>
            <field name="res_model">additional_vendors.vendor</field>
            <field name="view_mode">tree,form</field>
        </record>
    </data>
</odoo>